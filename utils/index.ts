export function getKaminoku1(tanka: string): string {
  const lines = tanka.split(/\s+/);
  return lines[0];
}
export function getKaminoku2(tanka: string): string {
  const lines = tanka.split(/\s+/);
  return lines[1];
}
export function getKaminoku3(tanka: string): string {
  const lines = tanka.split(/\s+/);
  return lines[2];
}
export function getShimonoku1(tanka: string): string {
  const lines = tanka.split(/\s+/);
  return lines[3];
}
export function getShimonoku2(tanka: string): string {
  const lines = tanka.split(/\s+/);
  return lines[4];
}
